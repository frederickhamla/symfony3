<section id="contact" class="section-five">
    <div class="form-container">
        <div class="form-contact">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
        {#section social#}
        {{ include("ShagBundle:Default:social.html.twig") }}
    </div>
</section>
